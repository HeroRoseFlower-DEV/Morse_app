name: Android APK Build

on:
  workflow_dispatch:
  push:
    branches: [ "main", "master" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Repository
      uses: actions/checkout@v4

    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'
        cache: gradle

    - name: Create Mock google-services.json
      run: |
        mkdir -p app
        cat <<EOF > app/google-services.json
        {
          "project_info": {
            "project_number": "123456789012",
            "project_id": "mock-app",
            "storage_bucket": "mock-app.appspot.com"
          },
          "client": [
            {
              "client_info": {
                "mobilesdk_app_id": "1:123456789012:android:1234567890123456",
                "android_client_info": {
                  "package_name": "com.aistudio.morsetranslator.mrsxqz"
                }
              },
              "oauth_client": [],
              "api_key": [
                {
                  "current_key": "AIzaSyMockKeyForBuildingApkOnly"
                }
              ],
              "services": {}
            }
          ],
          "configuration_version": "1"
        }
        EOF

    - name: Generate Debug Keystore
      run: keytool -genkey -v -keystore debug.keystore -storepass android -alias androiddebugkey -keypass android -keyalg RSA -keysize 2048 -validity 10000 -dname "C=US, O=Android, CN=Android Debug"

    - name: Ensure Gradle Wrapper exists
      run: gradle wrapper

    - name: Grant execute permission for gradlew
      run: chmod +x gradlew

    - name: Build Debug APK (Headless Mode)
      # اضافه کردن دستورات headless و no-daemon برای جلوگیری از کرش گرافیکی
      run: ./gradlew assembleDebug -Djava.awt.headless=true --no-daemon

    - name: Upload APK
      uses: actions/upload-artifact@v4
      with:
        name: Morse_app-APK
        path: app/build/outputs/apk/**/*.apk
